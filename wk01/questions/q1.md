We define Z-values for 2 ≤ i ≤ n, but what about Z1? What would it be for the
example above? Is this always the case? How much does this tell us about the
structure of the string?

Example: aabcaabxaay

For the example Z[1] = 11
We can set Z[1] equal to the length of the string because the substring starting
at 1 is the prefix which is always the prefix of the whole string.
So it can tell us how long the string is.
It is not useful for us though compared to the other z values after it.
